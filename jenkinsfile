pipeline {
    agent any

    environment {
        LABS = credentials('lab')
        JAVA_HOME = '/opt/bitnami/java'
        PATH = "${env.JAVA_HOME}/bin:${env.PATH}"
    }

    stages {
        stage('Verify Python & Venv') {
            steps {
                script {
                    sh '''
                    if ! python3 -m ensurepip --version &>/dev/null; then
                        echo "ERROR: python3-venv is not installed. Please install it manually."
                        exit 1
                    else
                        echo "python3-venv is installed."
                    fi
                    '''
                }
            }
        }

        stage('Checkout Code') {
            steps {
                checkout scm
            }
        }

        stage('Setup Virtual Environment') {
            steps {
                script {
                    sh 'rm -rf retail_pipeline_venv'
                    sh 'python3 -m venv retail_pipeline_venv || (echo "Virtualenv creation failed. Check python3-venv installation!" && exit 1)'
                    sh './retail_pipeline_venv/bin/pip install --upgrade pip'
                    sh './retail_pipeline_venv/bin/pip install pipenv'
                }
            }
        }

        stage('Install Dependencies') {
            steps {
                script {
                    sh './retail_pipeline_venv/bin/pipenv install'
                }
            }
        }

        stage('Run Tests') {
            steps {
                script {
                    sh './retail_pipeline_venv/bin/pipenv run pytest'
                }
            }
        }

        stage('Package Application') {
            steps {
                script {
                    sh 'zip -r retailproject.zip . -x "retail_pipeline_venv/*"'
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    sh '''
                    sshpass -p "$LABS_PSW" scp -o StrictHostKeyChecking=no -r \
                    retailproject.zip "$LABS_USR"@g01.itversity.com:/home/itv013301/retailproject
                    '''
                }
            }
        }
    }
}
